# v1 Platform Trust Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. **Can run in parallel** with content/sim tracks. Do not block NAT work on this. Authenticode signing is **not** in this plan (needs a purchased cert).

**Status:** Task 1 (`openios-vs-ios.md`) and first-run (presence plan) are **DONE**
on `master`. Remaining: typed-core mypy (Task 2+) and Linux notes. Do not redo Task 1.

**Goal:** A new user understands what OpenIOS actually simulates, CI types more of the engine, first launch picks a cert, and Linux packaging is documented even if unsigned Windows remains.

**Architecture:** Docs first (OpenIOS vs IOS). Then typed-core expansion one module at a time (no weakening mypy). Then `AppSettings.onboarding_complete: bool` + a modal in `MainWindow` that calls existing `navigate_practice` / `start_lab_from_list`. Linux: document `pip install -e ".[all]"` and `openboson gui`; optional PyInstaller notes only if `scripts/build_windows.ps1` is easy to twin — do not invent a full Linux installer.

**Tech Stack:** mypy, PySide6, settings JSON, GitHub Actions.

---

### Task 1: `docs/openios-vs-ios.md`

**Files:**
- Create: `docs/openios-vs-ios.md`
- Modify: `README.md` (one link under Status)
- Modify: `docs/openios-command-matrix.md` (link at top)

Write a one-pager with a table:

| Feature | OpenIOS does | OpenIOS does not |
|---------|--------------|------------------|
| Ping | L2 VLAN + static/OSPF + ACL + PAT + DHCP lease + PortFast + EC bundle | Real RTT, IPv6 ping, named ICMP types beyond echo |
| NAT | Inside/outside + overload required for inside PC → outside IP | Translation table, PAT ports, `show ip nat translations` |
| DHCP | One pool, excluded range, `ipconfig /renew` | Relay, snooping, conflict logging |
| STP | Access port forwarding requires PortFast | PVST, root election, BPDUs, storms |
| EtherChannel | Matching group = one logical link | LACP negotiation, load-balance hash |
| OSPF | Adjacent speakers install O routes | Areas, DR/BDR, LSAs |

- [x] Commit `docs: document OpenIOS scope versus real IOS`

---

### Task 2: Expand typed-core mypy

**Files:**
- Modify: `Makefile` `typecheck` target
- Modify: `.github/workflows/ci.yml` typing job (must stay in lockstep with Makefile)
- Modify: `docs/quality-baseline.md`

Add **one file per commit**, in this order, fixing only errors in that file:

1. `src/openboson/netsim/ios/device.py`
2. `src/openboson/stats_service.py` (if clean enough)
3. `src/openboson/netsim/grader.py`

Do **not** add `world.py` or `shell.py` until device.py is green (they are large). If a file needs more than ~15 annotation fixes, stop and document it as still excluded.

Commands:

```bash
python -m mypy src/openboson/netsim/ios/device.py
```

Expected: exit 0 before adding it to Makefile.

- [ ] Commit per file: `Add netsim/ios/device.py to mypy typed-core.`

---

### Task 3: First-run cert pick

**Files:**
- Modify: `src/openboson/settings_store.py` (`AppSettings`)
- Modify: `src/openboson/gui/main_window.py`
- Test: `tests/test_settings_store.py`, `tests/gui/test_main_window.py`

```python
# AppSettings
onboarding_complete: bool = False
preferred_cert: str | None = None  # "ccna" | "ccnp"
```

Update `_coerce` to read the new fields (unknown keys already dropped).

On `MainWindow` show after UI exists, if not `onboarding_complete` and not `OPENBOSON_SKIP_ONBOARDING=1`:

`QMessageBox` or a small `QDialog`:

- Title: `Welcome to OpenBoson`
- Buttons: `Study CCNA` / `Study ENCOR` / `Skip`
- CCNA → `preferred_cert="ccna"`, `onboarding_complete=True`, `navigate_practice(cert="ccna")`
- ENCOR → `ccnp`
- Skip → `onboarding_complete=True` only

Tests: `load_settings` round-trip; GUI test with `OPENBOSON_SKIP_ONBOARDING=1` (isolated_home) does not show dialog. One test with the flag unset and `onboarding_complete=False` can invoke the dialog with `qtbot` if existing main_window tests already construct `MainWindow`.

- [ ] Commit `Add a first-run CCNA/ENCOR choice.`

---

### Task 4: Linux runbook (no fake installer)

**Files:**
- Modify: `README.md` Quick start
- Create: `docs/linux.md` (short)

```markdown
# Linux

Python 3.11+ with Qt libs (see CI apt packages in `.github/workflows/ci.yml`).

python -m pip install -e ".[all]"
openboson gui
pytest -v
```

Do not claim a `.deb` exists. Windows SmartScreen stays documented in SUPPORT.md.

- [ ] Commit `Document Linux install from source.`

---

### Task 5: Handoff docs

**Files:** `docs/status.md`, `docs/deferred-releases.md`

- Status date → today; current work = Labs Quality honesty + study loop.
- Mark NAT/DHCP/STP packet effects as in progress or done based on merged tracks.
- v1.0 still lists Authenticode + macOS packages as later.

- [ ] Commit `Sync status and deferred-releases with the 2026-09 roadmap.`

---

### Exit

Unsigned Windows installer remains. No ENARSI. No pack store.
